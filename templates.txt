<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Solar Buddy ☀️</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f6d365 0%, #fda085 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .chat-container {
            background: #fff;
            border-radius: 15px;
            width: 500px;
            max-height: 85vh;
            overflow-y: auto;
            padding: 20px;
            box-shadow: 0 0 15px rgba(0,0,0,0.2);
        }
        .message {
            margin-bottom: 12px;
        }
        .user {
            color: #1E90FF;
            font-weight: bold;
        }
        .bot {
            color: #FF8C00;
            font-weight: bold;
        }
        form {
            margin-top: 15px;
            display: flex;
            gap: 10px;
        }
        input[type=text] {
            flex: 1;
            padding: 10px;
            border-radius: 10px;
            border: 1px solid #ccc;
        }
        button {
            background: #ff9a44;
            border: none;
            color: white;
            padding: 10px 15px;
            border-radius: 10px;
            cursor: pointer;
            transition: 0.3s;
        }
        button:hover {
            background: #ff7400;
        }
    </style>
</head>
<body>
    <div class="chat-container">
        <h2>☀️ Solar Buddy Chat</h2>
        <div id="chat-box">
            {% for speaker, msg in chat_history %}
                <div class="message"><span class="{{ 'user' if speaker == 'You' else 'bot' }}">{{ speaker }}:</span> {{ msg }}</div>
            {% endfor %}
        </div>

        <form method="POST">
            <input type="text" name="prompt" placeholder="Ask something about solar energy..." required>
            <button type="submit">Send</button>
        </form>
    </div>
</body>
</html>
